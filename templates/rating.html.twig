<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Rating</title>
  <link href="https://fonts.googleapis.com/css2?family=Miltonian+Tattoo&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Russo+One&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/beercss@3.5.8/dist/cdn/beer.min.css" rel="stylesheet">
  <link rel="stylesheet" href="css/rating-style.css">
</head>
<body>
<div class="table">
  <div class="table__multi">
    <h2>Многопользовательский режим</h2>
  </div>
  <div class="table__single">
    <h2>Однопользовательский режим</h2>
    <div class="table__levels">
      <div class="table__first-level">
        <h3>1 уровень</h3>
        <ol class="table__list">
          {% for user in usersFirstLevel %}
            <li> {{ user.username }}: {{ user.scoreFirstLevel }} </li>
          {% endfor %}
        </ol>
      </div>
      <div class="table__second-level">
        <h3>2 уровень</h3>
        <ol class="table__list">
          {% for user in usersSecondLevel %}
            <li> {{ user.username }}: {{ user.scoreSecondLevel }} </li>
          {% endfor %}
        </ol>
      </div>
      <div class="table__third-level">
        <h3>3 уровень</h3>
        <ol class="table__list">
          {% for user in usersThirdLevel %}
            <li> {{ user.username }}: {{ user.scoreThirdLevel }} </li>
          {% endfor %}
        </ol>
      </div>
    </div>
  </div>
</div>
<a class="previous-page" href="/menu">Вернуться</a>
</body>
</html>